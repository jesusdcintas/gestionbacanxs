---
import AppLayout from '../../layouts/AppLayout.astro';
import EventosTable from '../../components/eventos/EventosTable.tsx';
import { Button } from '../../components/ui/Button.tsx';
import { Card } from '../../components/ui/Card.tsx';
import { Plus } from 'lucide-react';
import { getEventosConEstadoReparto } from '../../services/eventos';

const eventos = await getEventosConEstadoReparto(Astro);
---

<AppLayout title="Eventos">
  <div class="space-y-6">
    <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
      <div>
        <h1 class="text-2xl uppercase italic sm:text-3xl" style="font-family: 'Archivo Black', sans-serif;">Eventos</h1>
        <p class="text-text-secondary mt-1">Gestiona tus conciertos y actuaciones</p>
      </div>
      <a href="/eventos/nuevo" class="w-full sm:w-auto">
        <Button variant="primary" client:load>
          <Plus size={20} />
          Nuevo Evento
        </Button>
      </a>
    </div>

    <Card>
      <EventosTable eventos={eventos} editBasePath="/eventos" client:load />
    </Card>
  </div>
</AppLayout>
